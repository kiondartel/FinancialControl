import React from "react";
import * as Styled from "./styles";

const Header = () => {
  return (
    <Styled.Container>
      <Styled.Header>
        <Styled.Title>Finance Control</Styled.Title>
      </Styled.Header>
    </Styled.Container>
  );
};

export default Header;
