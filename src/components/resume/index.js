import ResumeItem from "../ResumeItem";
import * as Styled from "./styles";

const Resume = () => {
  return (
    <Styled.Container>
      <ResumeItem />
      <ResumeItem />
      <ResumeItem />
    </Styled.Container>
  );
};

export default Resume;
